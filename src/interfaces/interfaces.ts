export interface SidebarItem {
  icon: string;
  nameProduct: string;
  id: string;
}

export interface HeroHomeSwiper {
  name: string;
  price: string;
  pieces: string;
  weight: string;
  img: { default: string };
}

export interface Product {
  name: string;
  size: string;
  weight: string;
  price: string;
}
